apply plugin: 'groovy'
apply from: 'localPublish.gradle'

group = 'com.cisco.gradle'
version = '1.0'

ext {
    publishName = 'gcov'
    publishHomepage = 'https://github.com/awrichar/gradle-gcov'
}

targetCompatibility = '1.6'

dependencies {
    compile localGroovy()
    compile gradleApi()
}

